<%= form_with(model: @category, local: true) do |form| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% end %>

<table id="example2" class="table table-bordered table-hover">     
	<thead>
    	<tr>
    		<th>#</th>
			<th>Name</th>
			<th>Description</th>
			<th colspan="1"></th>
        </tr>
	</thead>
	<tbody>
	    <tr>
	      <td><%= best_in_place @category, :id, :as => :input, :type => :textarea %></td>
	      <td><%= best_in_place @category, :name, :as => :input, :type => :textarea %></td>
		  <td><%= best_in_place @category, :description, :as => :input, :type => :textarea %></td>
	      <td><%= #link_to 'Destroy', navigation_path(@navigation), method: :delete, data: { confirm: 'Are you sure?' }  #, :place_holder => "0" 
	      %></td>
	    </tr>
    </tbody>
    <tfoot>
   	</tfoot>
</table>